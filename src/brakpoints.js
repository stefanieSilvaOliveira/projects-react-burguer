const size = {
    sm: '511px',
    md: '600px',
    bg: '966'
}

const breakpoints = {
    sm: `(max-width: ${size.sm})`,
    md: `(max-width: ${size.sm})`,
    bg: `(max-width: ${size.sm})`
}

export default breakpoints;